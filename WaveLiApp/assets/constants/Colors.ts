// Light mode

const primaryBrandBG = '#386641';
const primaryBrandText = '#91AA96';

const secondaryBrand = '#6A994E';

const tertiaryBrand = '#A7C957';

const errorBG = '#BC4749';
const errorText = '#DDA2A4';

const dark = '#252522';

const bodyText = '#4B4B48';

const accent = '#F2E8CF';

const light = '#F8F5F0';

const tintColorLight = '#2f95dc';
const tintColorDark = '#fff';

export default {
  light: {
    text: bodyText,
    background: light,
    tint: accent,
    tabIconSelected: dark,
  },
  dark: {
    text: '#fff',
    background: '#000',
    tint: tintColorDark,
    tabIconSelected: tintColorDark,
  },
};
